# Prototype

This directory holds the validated prototype pieces while the Rust workspace
catches up.

- `native-hook/`: Windows x86 C++ hook and injector.
- `tools/`: Python and PowerShell scripts used during validation.

These files are kept as working references, not as the final user experience.
